# =============================================================================
# Dockerfile.customer — Multi-stage build for {{ ecu.name_upper }} (customer ECU)
#
# Auto-generated by Taktflow SWC Onboarding Layer.
# DO NOT EDIT — regenerate from manifest.
#
# Build:  docker build -f Dockerfile.customer -t taktflow/{{ ecu.name }} .
# Run:    docker run --network host --cap-add NET_ADMIN taktflow/{{ ecu.name }}
# =============================================================================

# ---------------------------------------------------------------------------
# Build stage
# ---------------------------------------------------------------------------
FROM ubuntu:22.04 AS builder

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    can-utils \
    libsocketcan-dev \
    iproute2 \
    && rm -rf /var/lib/apt/lists/*

COPY . /build/
WORKDIR /build

RUN make -f Makefile.customer

# ---------------------------------------------------------------------------
# Runtime stage
# ---------------------------------------------------------------------------
FROM ubuntu:22.04

RUN apt-get update && apt-get install -y --no-install-recommends \
    can-utils \
    iproute2 \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /build/build/{{ ecu.name }}_posix /usr/local/bin/{{ ecu.name }}

ENV CAN_INTERFACE=vcan0

ENTRYPOINT ["/usr/local/bin/{{ ecu.name }}"]
