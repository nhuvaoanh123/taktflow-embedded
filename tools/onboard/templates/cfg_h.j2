/**
 * @file    {{ ecu.name_pascal }}_Cfg.h
 * @brief   {{ ecu.name_upper }} configuration — signal IDs, PDU IDs, counts
 * @date    {{ date }}
 *
 * @details Auto-generated by Taktflow SWC Onboarding Layer.
 *          DO NOT EDIT — regenerate from manifest.
 *
 * @standard AUTOSAR, ISO 26262 Part 6 (QM)
 * @copyright Taktflow Systems 2026
 */
#ifndef {{ ecu.name_upper }}_CFG_H
#define {{ ecu.name_upper }}_CFG_H

/* ====================================================================
 * RTE Signal IDs (offset 16 from BSW well-known)
 * ==================================================================== */

{% for sig in ecu.rte_signals -%}
#define {{ sig.macro_name }}    {{ sig.id }}u
{% endfor -%}
#define {{ ecu.name_upper }}_SIG_COUNT    {{ sig_count }}u

/* ====================================================================
 * Com TX PDU IDs
 * ==================================================================== */

{% for pdu in ecu.tx_pdus -%}
#define {{ pdu.pdu_macro }}    {{ pdu.pdu_value }}u  /* CAN 0x{{ '%03X' % pdu.can_id }} */
{% endfor %}
/* ====================================================================
 * Com RX PDU IDs
 * ==================================================================== */

{% for pdu in ecu.rx_pdus -%}
#define {{ pdu.pdu_macro }}    {{ pdu.pdu_value }}u  /* CAN 0x{{ '%03X' % pdu.can_id }} */
{% endfor %}
#endif /* {{ ecu.name_upper }}_CFG_H */
